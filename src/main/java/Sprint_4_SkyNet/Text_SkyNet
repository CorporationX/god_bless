Вы разрабатываете нейронную сеть SkyNet, управляющую всеми роботами-убийцами.
Ваша задача: написать такой код, чтобы разные роботы не могли атаковать
одну и ту же цель одновременно, это непроизводительно, так как каждый робот
стреляет точно в цель.

Для решения этой проблемы необходимо использовать synchronized, чтобы гарантировать,
что только один робот может атаковать цель в любой момент времени.

Создать класс Robot с приватным полем target (цель атаки) и методом attack,
в котором будет описана логика атаки на цель.

Создать класс SkyNetc методом main.

В методе main создать два объекта класса Robot, передав
им в конструкторы одинаковую цель для атаки.

Создать два потока, каждый из которых будет вызывать метод attack
соответствующего робота.

Запустить потоки методом start()