package faang.school.godbless.streamapi2;

import java.util.List;

public class StreamAPI2 {
    public static void pairsnumbers(List<Integer> numberList, int numberCondition) {
        /*List<Integer,Integer>
        numberList.stream().distinct().sorted().flatMap(num -> {
            numberList.forEach();
        })*/
    }
}
