package school.faang.streams.stream2.hogwartshogwartsourbelovedhogwarts;

import org.junit.jupiter.api.Test;

public class SpellCasterTest {

    @Test
    void test() {
        SpellCaster spellCaster = new SpellCaster();

        String zarplatus200KInNanoSecPoluchatus = "zarplatus200KInNanoSecPoluchatus";

        spellCaster.cast(zarplatus200KInNanoSecPoluchatus, (spell) -> "Salary 200kInNanoSec received by " + spell);
    }
}
